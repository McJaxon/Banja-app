import 'package:flutter/material.dart';
import 'package:flutter_screenutil/flutter_screenutil.dart';

/// #####  TextStyle for home page title
/// Here we are setting the text style to font family `Poppins` and font size to
/// `30.0` from the `package:flutter_screenutil`, font weight to `600` and text color to `white70` to add a glass effect
TextStyle homePageTextStyle =  TextStyle(
    fontFamily: 'Poppins',
    fontWeight: FontWeight.w600,
    fontSize: 36.sp,
    color: Colors.black);

/// #####  TextStyle for home page amount figure
/// Here we are setting the text style to font family `Poppins` and font size to
/// `60.0` from the `package:flutter_screenutil`, font weight to `800` and text color to `white70` to add a glass effect
TextStyle amountTextStyle = TextStyle(
    fontFamily: 'Poppins',
    fontWeight: FontWeight.w800,
    fontSize: 60.0,
    color: Colors.white70);

/// #####  TextStyle for home page currency text
/// Here we are setting the text style to font family `Poppins` and font size to
/// `18.0` from the `package:flutter_screenutil`, font weight to `600` and text color to `white38` to add a glass effect
TextStyle currencyTextStyle = TextStyle(
    fontFamily: 'Poppins',
    fontWeight: FontWeight.w600,
    fontSize: 18.0,
    color: Colors.white38);

/// #####  TextStyle for home page amount buttons
/// Here we are setting the text style to font family `Poppins` and font size to
/// `26.0` from the `package:flutter_screenutil`, font weight to `800` and text color to `white54` to add a glass effect
TextStyle amountInputButtonTextStyle = TextStyle(
    color: Colors.black,
    fontSize: 34.sp,
    fontFamily: 'Poppins',
    fontWeight: FontWeight.w400);

/// #####  TextStyle for home page action button
/// Here we are setting the text style to font family `Poppins` and font size to
/// `20.0` from the `package:flutter_screenutil`, font weight to `600` and text color to `white`
TextStyle homeActionButtonTextStyle = TextStyle(
    color: Colors.white,
    fontSize: 20.0,
    fontFamily: 'Poppins',
    fontWeight: FontWeight.w600);

/// #####  TextStyle for dialog title text
/// Here we are setting the text style to font family `Poppins` and font size to
/// `34.0` from the `package:flutter_screenutil`, font weight to `800` and text color to `black`
TextStyle dialogTitleTextStyle = TextStyle(
    fontFamily: 'Poppins', fontSize: 34.0, fontWeight: FontWeight.w800);

/// #####  TextStyle for dialog body text
/// Here we are setting the text style to font family `Poppins` and font size to
/// `22.0` from the `package:flutter_screenutil`, font weight to `500` and text color to `black`
TextStyle dialogBodyTextStyle = TextStyle(
    fontFamily: 'Poppins', fontSize: 22.0, fontWeight: FontWeight.w500);

/// #####  TextStyle for dialog button text
/// Here we are setting the text style to font family `Poppins` and font size to
/// `24.0` from the `package:flutter_screenutil`, font weight to `600` and text color to `white`
TextStyle dialogButtonTextStyle = TextStyle(
    fontFamily: 'Poppins',
    fontSize: 24.0,
    color: Colors.white,
    fontWeight: FontWeight.w600);

/// #####  TextStyle for bottom navigation bar text
/// Here we are setting the text style to font family `Poppins` and font size to
/// `14.0` from the `package:flutter_screenutil`, font weight to `Bold`
TextStyle navBarSelectedTabTextStyle = TextStyle(
  fontSize: 14.0,
  fontWeight: FontWeight.bold,
  fontFamily: 'Poppins',
);

/// #####  TextStyle for bottom navigation bar  unselected text
/// Here we are setting the text style to font family `Poppins` and font size to
/// `14.0` from the `package:flutter_screenutil`, font weight to `Bold`
TextStyle navBarUnSelectedTabTextStyle = TextStyle(
    fontSize: 14.0, fontWeight: FontWeight.bold, fontFamily: 'Poppins');

TextStyle onBoardingHeavy = TextStyle(
    fontFamily: 'Poppins',
    fontWeight: FontWeight.w700,
    color: Colors.white,
    fontSize: 34.sp);

TextStyle onBoardingDescription = TextStyle(
    fontFamily: 'Poppins',
    fontWeight: FontWeight.w400,
    color: Colors.white,
    fontSize: 18.sp);

TextStyle onBoardingButton = TextStyle(
    fontFamily: 'Poppins',
    fontWeight: FontWeight.w700,
    color: Colors.white,
    fontSize: 18.sp);

TextStyle textButtonStyle = TextStyle(
    fontFamily: 'Poppins',
    fontWeight: FontWeight.w700,
    color: const Color(0xff007981),
    fontSize: 15.sp);

TextStyle companyDetailsTitleStyle = TextStyle(
    fontWeight: FontWeight.w700, fontFamily: 'Poppins', fontSize: 21.5.sp);

TextStyle companyDetailsHeadingStyle = TextStyle(
    color: const Color.fromARGB(255, 63, 110, 110),
    fontSize: 34.sp,
    fontFamily: 'Pacifico');

TextStyle companyDetailsBodyStyle =
    TextStyle(fontFamily: 'Poppins', fontSize: 19.5.sp);

TextStyle companyDetailsHeavy = TextStyle(
    fontWeight: FontWeight.w700, fontFamily: 'Poppins', fontSize: 17.5.sp);

TextStyle companyDetailsLight = TextStyle(
    fontWeight: FontWeight.w500, fontFamily: 'Poppins', fontSize: 16.5.sp);
